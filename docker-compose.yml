version: '3.1'
services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: mysql_root
      MYSQL_DATABASE: algosolve
      MYSQL_USER: mysql
      MYSQL_PASSWORD: mysql
      TZ: Asia/Seoul
    ports:
      - "3306:3306"
